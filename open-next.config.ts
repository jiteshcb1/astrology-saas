import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Minimal OpenNext Cloudflare config for the scaffold.
// Incremental cache / R2 cache overrides can be added later if needed.
export default defineCloudflareConfig();
